# ─────────────────────────────────────────────────────────────────────────────
# 小麦（XiaoMai）环境变量示例
# ─────────────────────────────────────────────────────────────────────────────
# 复制本文件为 `.env` 用于本地配置。
# 切勿将包含密钥的 `.env` 文件提交到版本控制系统。
#
# 说明：站点地址、Umami 统计、字体模式、音乐、侧边栏等主题配置，
# 走「内容仓 config/*.yaml 覆盖层」(withUserConfig)，不通过环境变量注入。
# 详见 docs/content-separation/config-overlay.md 与 docs/umami-guide.md。

# ─────────────────────────────────────────────────────────────────────────────
# 一、内容分离（本地开发 / 双仓模式）
# 详见 docs/content-separation.md
# ─────────────────────────────────────────────────────────────────────────────
# 全部留空则保持 `local` 模式：使用代码仓自带内容，且 `pnpm content:sync` 为空操作。
# 来源优先级：XIAOMAI_CONTENT_SYNC=0  >  CONTENT_DIR  >  CONTENT_REPO_URL  >  xiaomai.content.json 的 source。

# [可选] 本地内容仓检出路径（绝对或相对均可）。优先级最高；
#        适合在第二个终端运行 `pnpm content:watch` 时于本地撰写文章。
# CONTENT_DIR="../xiaomai-content"

# [可选] 远程内容仓 Git 地址。适用于直接构建代码仓、因而无法自行检出第二仓的托管环境。
#        若内容仓为私有，可将令牌嵌入 URL：https://x-access-token:<TOKEN>@github.com/GrowWheat/REPO.git
#        （令牌会在所有日志中自动脱敏，不会泄露）。
# CONTENT_REPO_URL="https://github.com/GrowWheat/xiaomai-content.git"
# [可选] 远程内容仓的分支 / 引用，默认 main。
# CONTENT_REPO_REF="main"

# [可选] 设为 "false" 可复用已有的 `.content-src/` 工作副本而无需重新拉取。
#        当 CI 作业已检出内容时很有用。
# CONTENT_SYNC_PULL="true"

# [可选] 设为 "0" / "false" / "off" / "no" 可强制进入 `local` 模式，
#        即使上方设置或 `xiaomai.content.json` 存在。
# XIAOMAI_CONTENT_SYNC="1"

# ─────────────────────────────────────────────────────────────────────────────
# 二、字体预算上限（单位：字节，构建期校验）
# 覆盖 src/config/fontConfig 中的默认预算；仅 `pnpm fonts:check` 与 `build` 末尾校验时读取。
# ─────────────────────────────────────────────────────────────────────────────
# FONT_MAX_TOTAL_BYTES=4194304
# FONT_MAX_FAMILY_BYTES=2097152

# ─────────────────────────────────────────────────────────────────────────────
# 三、本地工具 / 测试
# ─────────────────────────────────────────────────────────────────────────────
# [可选] Lighthouse 设备类型：mobile 或 desktop。
#        由 lighthouserc.cjs 与 `pnpm lighthouse:mobile|desktop` 读取。
# LH_DEVICE="desktop"

# CI 由持续集成环境自动注入（scripts/content/export.mjs 据此调整行为），一般无需手动设置。

# ─────────────────────────────────────────────────────────────────────────────
# 四、CI/CD 与部署密钥（GitHub Actions Secrets，不写入本地 .env）
# 以下均在仓库「Settings > Secrets and variables > Actions」中配置，
# 供 .github/workflows/*.yml 与 .github/workflows/deploy.yml.example 使用。
# ─────────────────────────────────────────────────────────────────────────────
# 代码仓 / 部署使用：
#   CONTENT_REPO_TOKEN   克隆私有内容仓所需的令牌（内容仓为公开仓库时可省略）
#   THEME_REPO_TOKEN     主题仓（代码仓）访问令牌（部分部署方式需要；个别工作流中也写作 theme_token）
#   VERCEL_TOKEN         Vercel 部署令牌
#   VERCEL_ORG_ID        Vercel 组织 ID
#   VERCEL_PROJECT_ID    Vercel 项目 ID
#   DEPLOY_SSH_KEY       通过 SSH 部署到自有服务器时的私钥
#   DEPLOY_HOST          部署目标主机地址
#   DEPLOY_USER          部署目标用户名
#   DEPLOY_HOOK          部署完成后的回调 / Webhook 地址
#
# 内容仓使用（跨仓触发代码仓构建）：
#   DISPATCH_TOKEN       对代码仓拥有读写权限的个人访问令牌（PAT）
#
# 可选、当前代码尚未消费（deploy.yml.example 已预留，anime 同步脚本尚未实现）：
#   BILI_SESSDATA        B 站登录态 SESSDATA，用于构建期同步番剧数据
